# YOLOv8 🚀, GPL-3.0 license

# REQUIRED: This line tells Ultralytics what type of tracker this config is for
tracker_type: deepocsort 

# === Parameters for DeepOCSORT tracker (Rebalanced for Handling Occlusion) ===

# --- Core Parameters to Tune ---

det_thresh: 0.45 # ค่าเดิม 0.45 ยังคงเหมาะสม

# *** พารามิเตอร์ที่สำคัญที่สุดสำหรับปัญหารถโดนบัง (Occlusion) ***
# จำนวนเฟรมสูงสุดที่ Tracker จะยังจำ ID ของรถที่หายไป (เช่น โดนบัง) ได้
# เราจะเพิ่มค่านี้กลับขึ้นมา เพื่อให้ Tracker มีเวลารอรถโผล่ออกมาจากที่บังนานขึ้น
# แต่จะไม่เพิ่มกลับไปสูงเท่าค่าเริ่มต้น (30) เพื่อยังคงป้องกันปัญหา ID สลับได้ในระดับหนึ่ง
# ลองปรับในช่วง: 20 - 30 (ค่าเดิมคือ 30, ค่าล่าสุดคือ 15)
max_age: 30 # เดิม: 15

# จำนวนเฟรมขั้นต่ำที่ต้องเจอรถติดต่อกัน ถึงจะยอมสร้าง Track ID ใหม่ให้
min_hits: 3 # ค่านี้เหมาะสมดีแล้ว

# --- Association Parameters (การจับคู่ Track) ---

iou_thresh: 0.3 # ค่านี้เหมาะสมดีแล้ว

asso_func: iou # เหมาะสมดีแล้ว

# ค่าน้ำหนักระหว่าง "ลักษณะ" (Appearance) กับ "การเคลื่อนที่" (Motion)
# การคงค่านี้ไว้สูงๆ จะช่วยให้ Tracker ตัดสินใจจาก "หน้าตา" ของรถได้ดีขึ้นเมื่อมันโผล่ออกมาอีกครั้ง
w_association_emb: 0.90 # เดิม: 0.90 (ยังคงค่านี้ไว้สูงเพื่อช่วยในการ Re-identification)

# --- Other Parameters (มักไม่ต้องปรับบ่อย) ---

inertia: 0.2
alpha_fixed_emb: 0.95
aw_param: 0.5
embedding_off: False
cmc_off: False
aw_off: False
delta_t: 3
Q_xy_scaling: 0.01
Q_s_scaling: 0.0001
